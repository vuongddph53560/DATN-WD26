import { noop$1 as noop } from "./noop-8bk75rVj.mjs";

//#region src/utils/createDeferredPromise.ts
/**
* Create a promise that can be resolved or rejected from
* outside the Promise constructor callback
* A ES6 compatible utility that implements `Promise.withResolvers`
* @internal
*/
const createDeferredPromise = () => {
	let resolve = noop;
	let reject = noop;
	const promise = new Promise((res, rej) => {
		resolve = res;
		reject = rej;
	});
	return {
		promise,
		resolve,
		reject
	};
};

//#endregion
export { createDeferredPromise as createDeferredPromise$1 };
//# sourceMappingURL=createDeferredPromise-UKTFg5ib.mjs.map